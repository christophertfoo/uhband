<%= form_for(@letter_grade) do |f| %>
  <% if @letter_grade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@letter_grade.errors.count, "error") %> prohibited this letter_grade from being saved:</h2>

      <ul>
      <% @letter_grade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :grade %><br>
    <%= f.text_field :grade %>
  </div>
  <div class="field">
    <%= f.label :min_points %><br>
    <%= f.number_field :min_points %>
  </div>
  <div class="field">
    <%= f.label :max_points %><br>
    <%= f.number_field :max_points %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
